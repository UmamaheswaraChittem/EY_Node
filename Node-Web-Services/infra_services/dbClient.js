//const sql = require("mysql"); //import mysql,mssql e.t.c...whichever is available

//Test config if DB connection is available, we can connect as mentioned below
const dbConfig = {
    //To Do
};
//sample function to execute query
async function executeQuery(query, parameters) {
    //To DO
}

module.exports = { executeQuery }